[← Назад в Главное меню](../README.md)

# Артефакт 4. Концептуальная архитектура.

Учитывая транснациональный масштаб, гетерогенность технологий и огромные объемы 
данных спортивной социальной сети, монолитный подход к проектированию неприменим. 
Данная концептуальная архитектура базируется на разделении системы по изолированным 
бизнес-доменам (Bounded Contexts). Это позволяет различным командам разработчиков 
независимо развивать свои модули на разных языках программирования и разворачивать 
их в рамках гибридной Multi-Cloud инфраструктуры.

---

### Схема концептуальной архитектуры и потоков данных

Ниже представлена глобальная схема движения трафика и взаимодействия ключевых 
доменов системы — от внешних источников и IoT-устройств до изолированного слоя данных:

```mermaid
graph TD
    classDef domain fill:#e1f5fe,stroke:#0288d1,stroke-width:2px,font-weight:bold;
    classDef commerce fill:#e8f5e9,stroke:#388e3c,stroke-width:2px,font-weight:bold;
    classDef client fill:#ffffff,stroke:#333,stroke-width:1px;
    classDef gw fill:#fff3e0,stroke:#ffb74d,stroke-width:2px,font-weight:bold;
    classDef storage fill:#e8f5e9,stroke:#388e3c,stroke-width:2px;

    %% 1. УРОВЕНЬ ВНЕШНИХ ИСТОЧНИКОВ ТРАФИКА
    U_MOBILE(["Спортсмены по всему миру<br/>(Офлайн/Онлайн Мобильное приложение)"]):::client
    U_DEVICES(["Внешние фитнес-устройства<br/>(Датчики пульса, часов, смартфона)"]):::client
    U_B2B(["Дистрибьюторы и Поставщики<br/>(B2B Интеграции партнёров)"]):::client

    %% 2. СЛОЙ УПРАВЛЕНИЯ ТРАФИКОМ (API GATEWAYS)
    subgraph EDGE_CONTOUR ["СЛОЙ УПРАВЛЕНИЯ ТРАФИКОМ"]
        GW_B2C["Edge B2C Gateway<br/>[Вход для пользователей / REST API]"]:::gw
        GW_B2B["B2B Partner Gateway<br/>[Вход для дистрибьюторов / gRPC]"]:::gw
        GW_IoT["IoT Ingestion Gateway<br/>[Вход для телеметрии / MQTT]"]:::gw
    end

    %% Направление входящего трафика
    U_MOBILE ==>|HTTP/REST запросы| GW_B2C
    U_B2B ==>|Пакетные gRPC вызовы| GW_B2B
    U_DEVICES ==>|Ежесекундные MQTT сигналы| GW_IoT

    %% 3. ВНУТРЕННИЙ ПРИКЛАДНОЙ КОНТУР (ИЗОЛИРОВАННЫЕ ДОМЕНЫ)
    subgraph BACKEND_ZONE ["ВНУТРЕННИЙ ПРИКЛАДНОЙ КОНТУР ПРИЛОЖЕНИЯ"]
        DOM_SOCIAL["1. Домен Социализации<br/>(Управление группами, лента друзей)"]:::domain
        DOM_TELEMETRY["2. Домен Телеметрии<br/>(Пакетная обработка треков, Offline-First)"]:::domain
        DOM_GAMIFICATION["3. Домен Геймификации<br/>(Испытания, таблицы лидеров, ИИ-агент)"]:::domain
        DOM_COMMERCE["4. Домен Электронной коммерции<br/>(Износ инвентаря, маркетплейс, акции)"]:::commerce
    end

    %% Маршрутизация от шлюзов к доменам
    GW_B2C --> DOM_SOCIAL
    GW_B2C --> DOM_GAMIFICATION
    GW_B2B --> DOM_COMMERCE
    GW_IoT --> DOM_TELEMETRY

    %% Взаимодействие между прикладными доменами
    DOM_TELEMETRY ==>|Асинхронный сброс тренировок| DOM_GAMIFICATION
    DOM_TELEMETRY -.->|Координаты для поиска напарников в моменте| DOM_SOCIAL
    DOM_TELEMETRY ==>|Данные километража для расчета износа| DOM_COMMERCE
    DOM_GAMIFICATION ==>|Публикация спортивных триумфов и наград в ленту| DOM_SOCIAL
    DOM_COMMERCE ==>|Поставка контекстной региональной рекламы в ленту| DOM_SOCIAL
    DOM_GAMIFICATION ==>|Мотивация внесения инвентаря за игровую валюту| DOM_COMMERCE

    %% 4. ВЫДЕЛЕННЫЙ ИЗОЛИРОВАННЫЙ СЛОЙ ХРАНЕНИЯ ДАННЫХ (ПОЛИГЛОТНОЕ ХРАНЕНИЕ)
    subgraph STORAGE_ZONE ["КОНТУР ДАННЫХ (ПОЛИГЛОТНОЕ ХРАНЕНИЕ)"]
        DB_GRAPH[(Графовая СУБД<br/>Социальные связи и группы)]:::storage
        DB_TS[(Time-Series / Columnar СУБД<br/>Массивы IoT-телеметрии)]:::storage
        DB_RELATIONAL[(Реляционная СУБД<br/>Профили, заказы, инвентарь)]:::storage
    end

    %% Связи доменов с базами данных
    DOM_SOCIAL ---> DB_GRAPH
    DOM_TELEMETRY ---> DB_TS
    DOM_COMMERCE ---> DB_RELATIONAL
    DOM_GAMIFICATION ---> DB_RELATIONAL

    style EDGE_CONTOUR fill:#fffde7,stroke:#ffb74d
    style BACKEND_ZONE fill:#fcffff,stroke:#0288d1
    style STORAGE_ZONE fill:#f8fff9,stroke:#388e3c
```

---

### Архитектурное описание структуры и потоков данных:

1.  **Слой управления трафиком (API Gateways):**  
    Входящие потоки строго разделены на сетевом периметре для исключения влияния 
    пиковых нагрузок друг на друга. Розничные пользователи идут через легкий 
    HTTP-шлюз `Edge B2C`. Дистрибьюторы передают каталоги через производительный 
    gRPC-шлюз `B2B Partner`. Ежесекундные сигналы медицинских датчиков и часов 
    аккумулируются специализированным шлюзом `IoT Ingestion` по протоколу MQTT, 
    что защищает внутренние серверы от перегрузки сетевых соединений.
2.  **Децентрализация прикладного контура:**  
    Каждый из четырех доменов представляет собой самостоятельный контекст. Сервисы 
    домена Телеметрии сфокусированы исключительно на быстром приеме координат и пульса. 
    Они не знают о существовании маркетплейса, а лишь передают асинхронные сообщения 
    с километражем в домен Электронной коммерции, который изолированно рассчитывает 
    амортизацию кроссовок.
3.  **Полиглотное хранение данных (Polyglot Persistence):**  
    Вместо единой СУБД под разные типы данных выбраны специализированные решения:
    *   *Графовая СУБД* идеально приспособлена для мгновенного обхода цепочек друзей, 
        спортивных групп и поиска напарников поблизости.
    *   *Time-Series / Колоночная СУБД* оптимизирована под тяжелую ежесекундную 
        запись миллиардов точек GPS-треков и пульса от миллионов устройств по всему миру.
    *   *Реляционная СУБД* гарантирует строгую согласованность данных, транзакционность 
        и надежность при оформлении заказов, списании денег и управлении инвентарем.
